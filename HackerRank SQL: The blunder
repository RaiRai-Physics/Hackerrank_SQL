HackerRank SQL: The blunder

Question Link: https://www.hackerrank.com/challenges/the-blunder/problem?isFullScreen=true

select
truncate(diff, 0) + (diff > truncate(diff, 0))
from (
select
avg(salary) - avg(replace(salary, '0', '')) as diff
from employees
) t;
